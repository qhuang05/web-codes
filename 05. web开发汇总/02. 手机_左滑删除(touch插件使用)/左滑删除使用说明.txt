1. 使用touch.js插件时，要使用zepto-1.1.6.js版本(由于使用高版本v1.2.0时，swipe事件不生效)。
2. 使用touch.js插件时，触发swipe事件时，滑动时有垂直距离滑动时，页面会跟着滑动，
   导致swipe事件不灵敏。
3. 使用自定义的touch事件来解决swipe事件不灵敏问题。
   缺点：事件主体不支持数组, 数组元素操作须配合each使用(待改进)。



